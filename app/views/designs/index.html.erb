<!-- Page Title -->
<div class="section section-breadcrumbs">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <h1>Disennos</h1>
      </div>
    </div>
  </div>
</div>


<div class="section">
  <div class="container">
    <div class="row">
      <h4>Los diseños de <strong>Yoe Riba</strong> son realizados siempre en plena comunicación entre los clientes y el diseñador.
        Como resultado de un gran trabajo con dedicación se obtiene una variada muestra de diseños.</h4>
      <hr>

    </div>
  </div>
</div>

<div class="section">
  <div class="container">
    <div class="row">

      <ul class="grid cs-style-2">

        <% @designs.each do |design|%>
            <% image =design.image.url(:card)%>
            <%= render 'form', :image => image, :design => design %>
        <% end %>
      </ul>
    </div>

  </div>

</div>
<% if @designs.count > 6 %>
  <div class="apple_pagination">
    <%= will_paginate @designs, :container => true %>
  </div>
<% end %>